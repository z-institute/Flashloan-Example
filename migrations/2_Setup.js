const Setup = artifacts.require("Setup");

module.exports = function (deployer) {
  // deployer.deploy(Setup, { value: web3.utils.toWei("1050") });
  // deployer.deploy(Setup, { value: web3.utils.toWei("1050") });
};
